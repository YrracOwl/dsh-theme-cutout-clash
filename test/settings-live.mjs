import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9351/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('127.0.0.1:3080'));const w=new WebSocket(page.webSocketDebuggerUrl);let i=0,m=new Map();w.on('message',x=>{const a=JSON.parse(String(x));if(a.id){const q=m.get(a.id);if(q){m.delete(a.id);q(a.result)}}});await new Promise(r=>w.once('open',r));const s=(method,params={})=>new Promise(r=>{const id=++i;m.set(id,r);w.send(JSON.stringify({id,method,params}))});const e=async x=>(await s('Runtime.evaluate',{expression:x,returnByValue:true})).result.value;await s('Runtime.enable');console.log(JSON.stringify(await e(`(()=>[...document.querySelectorAll('button')].map((b,i)=>({i,text:(b.textContent||'').trim().slice(0,80),aria:b.getAttribute('aria-label'),title:b.title})).filter(x=>x.text.includes('设置')||x.aria?.includes('设置')||x.title.includes('设置')))()`),null,2));await e(`(()=>{const b=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').trim()==='设置');if(b)b.click();else document.querySelector('[aria-label="打开侧边栏"]')?.click()})()`);await new Promise(r=>setTimeout(r,900));console.log(JSON.stringify(await e(`(()=>({text:document.body.innerText.slice(-1800),row:document.body.innerText.includes('Cutout Clash'),buttons:[...document.querySelectorAll('button')].map(b=>(b.textContent||'').trim()).filter(x=>/Cutout|Night|Pop Signal|Acid Noir/.test(x))}))()`),null,2));w.close()
