import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const p=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(x=>x.type==='page'&&x.url.includes('3080'));const w=new WebSocket(p.webSocketDebuggerUrl);let i=0,m=new Map();w.on('message',x=>{let a=JSON.parse(String(x));if(a.id&&m.has(a.id)){let q=m.get(a.id);m.delete(a.id);q(a.result)}});await new Promise(r=>w.once('open',r));const s=(method,params={})=>new Promise(r=>{let id=++i;m.set(id,r);w.send(JSON.stringify({id,method,params}))});const ev=async exp=>(await s('Runtime.evaluate',{expression:exp,returnByValue:true})).result.value;await s('Runtime.enable');const snapshot=await ev(`(()=>{const info=e=>{if(!e)return null;const c=getComputedStyle(e),r=e.getBoundingClientRect();return{text:(e.textContent||'').replace(/\\s+/g,' ').trim().slice(0,120),color:c.color,bg:c.backgroundColor,aria:e.getAttribute('aria-label'),selected:e.getAttribute('aria-selected'),expanded:e.getAttribute('aria-expanded'),title:e.title,visible:c.visibility,rect:{x:r.x,y:r.y,w:r.width,h:r.height},html:e.outerHTML.slice(0,1000)}};return{selected:[...document.querySelectorAll('[data-pane="sidebar"] [role="treeitem"][aria-selected="true"], [data-pane="sidebar"] [role="treeitem"][aria-expanded="true"]')].map(info),sidebarBottom:[...document.querySelectorAll('[data-pane="sidebar"] button')].filter(e=>{const r=e.getBoundingClientRect();return r.y>innerHeight*.7}).map(info),details:[...document.querySelectorAll('[data-pane="details"] button, [data-dsh-surface="details"] button')].map(info).slice(0,50),subagentOpen:[...document.querySelectorAll('[role="tree"][aria-label="子代理会话"] [role="treeitem"]')].slice(0,3).map(info),shell:{frame:(()=>{const e=document.querySelector('[data-details-collapsed]');return info(e)})(),overlay:info(document.querySelector('[data-shell-overlay]')),plate:info(document.querySelector('.cc-plate')),details:info(document.querySelector('[class*="detailsCol"]'))}}})()`);console.log(JSON.stringify(snapshot,null,2));w.close()
