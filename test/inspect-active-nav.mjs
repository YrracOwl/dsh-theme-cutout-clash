import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const p=(await fetch('http://127.0.0.1:9352/json').then(r=>r.json())).find(x=>x.type==='page'&&x.url.includes('127.0.0.1:3080'));const w=new WebSocket(p.webSocketDebuggerUrl);let i=0,m=new Map();w.on('message',x=>{const a=JSON.parse(String(x));if(a.id){const q=m.get(a.id);if(q){m.delete(a.id);q(a.result)}}});await new Promise(r=>w.once('open',r));const s=(method,params={})=>new Promise(r=>{const id=++i;m.set(id,r);w.send(JSON.stringify({id,method,params}))});const r=await s('Runtime.evaluate',{expression:`[...document.querySelectorAll('button')].filter(b=>b.textContent.trim()==='通用设置').map(b=>b.outerHTML)`,returnByValue:true});console.log(JSON.stringify(r.result.result.value,null,2));w.close()
