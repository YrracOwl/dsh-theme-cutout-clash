import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9356/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('127.0.0.1:3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let seq=0;const pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){pending.get(m.id)(m.result);pending.delete(m.id)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise(r=>{const id=++seq;pending.set(id,r);ws.send(JSON.stringify({id,method,params}))});const e=async x=>(await send('Runtime.evaluate',{expression:x,returnByValue:true,awaitPromise:true})).result.value;const delay=ms=>new Promise(r=>setTimeout(r,ms));const href=await e(`(()=>{const links=[...document.querySelectorAll('[data-chat-flow] li a[href]')];const a=links[links.length-1];if(!a)return null;a.scrollIntoView({block:'center'});return a.href})()`);if(!href)throw new Error('no link');await delay(150);const box=await e(`(()=>{const a=[...document.querySelectorAll('[data-chat-flow] li a[href]')].find(a=>a.href===${JSON.stringify(href)});const r=a.getBoundingClientRect();return{x:r.left+r.width/2,y:r.top+r.height/2}})()`);await send('Input.dispatchMouseEvent',{type:'mouseMoved',x:box.x,y:box.y});await delay(120);const result=await e(`(()=>{const a=[...document.querySelectorAll('[data-chat-flow] li a[href]')].find(a=>a.href===${JSON.stringify(href)});const c=getComputedStyle(a);return{hover:a.matches(':hover'),color:c.color,line:c.textDecorationLine,decorationColor:c.textDecorationColor,thickness:c.textDecorationThickness,offset:c.textUnderlineOffset,background:c.backgroundColor}})()`);console.log(JSON.stringify(result,null,2));ws.close()