import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const label=process.argv.slice(2).join(' ');const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page');const ws=new WebSocket(page.webSocketDebuggerUrl);let i=0,m=new Map();ws.on('message',x=>{const a=JSON.parse(String(x));if(a.id&&m.has(a.id)){m.get(a.id)(a.result);m.delete(a.id)}});await new Promise(r=>ws.once('open',r));const s=(method,params={})=>new Promise(r=>{const id=++i;m.set(id,r);ws.send(JSON.stringify({id,method,params}))});const e=async x=>(await s('Runtime.evaluate',{expression:x,returnByValue:true})).result.value;await s('Runtime.enable');await e(`(()=>{const b=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').trim()==='设置');b?.click();return !!b})()`);await new Promise(r=>setTimeout(r,500));const result=await e(`(()=>{const bs=[...document.querySelectorAll('button')].filter(b=>(b.textContent||'').trim()===${JSON.stringify(label)});bs.at(-1)?.click();return{count:bs.length,labels:[...document.querySelectorAll('button')].map(b=>(b.textContent||'').trim()).filter(x=>/Cutout|Pop Signal/.test(x))}})()`);await new Promise(r=>setTimeout(r,700));result.body=await e(`document.body.getAttribute('data-cutout-clash')`);console.log(JSON.stringify(result,null,2));ws.close()
