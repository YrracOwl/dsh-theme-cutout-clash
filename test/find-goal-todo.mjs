import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const p=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(x=>x.type==='page'&&x.url.includes('3080'));const w=new WebSocket(p.webSocketDebuggerUrl);let i=0,m=new Map();w.on('message',x=>{const a=JSON.parse(String(x));if(a.id&&m.has(a.id)){const q=m.get(a.id);m.delete(a.id);q(a.result)}});await new Promise(r=>w.once('open',r));const s=(method,params={})=>new Promise(r=>{const id=++i;m.set(id,r);w.send(JSON.stringify({id,method,params}))});await s('Runtime.enable');const r=await s('Runtime.evaluate',{expression:`(()=>({title:document.title,heading:[...document.querySelectorAll('header,button')].map(e=>(e.textContent||'').replace(/\\s+/g,' ').trim()).filter(Boolean).slice(0,40),goalCount:document.querySelectorAll('[data-goal-bar]').length,todoCount:document.querySelectorAll('[data-testid="todo-panel"]').length,goalText:[...document.querySelectorAll('*')].filter(e=>(e.textContent||'').includes('进行中的目标')).slice(-10).map(e=>({tag:e.tagName,cls:String(e.className),text:(e.textContent||'').replace(/\\s+/g,' ').trim().slice(0,200)})),selected:[...document.querySelectorAll('[role="treeitem"][aria-selected="true"]')].map(e=>(e.textContent||'').replace(/\\s+/g,' ').trim())}))()`,returnByValue:true});console.log(JSON.stringify(r.result.value,null,2));w.close()
