import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('3080'));const w=new WebSocket(page.webSocketDebuggerUrl);let i=0,m=new Map();w.on('message',x=>{const a=JSON.parse(String(x));if(a.id&&m.has(a.id)){const q=m.get(a.id);m.delete(a.id);q(a.result)}});await new Promise(r=>w.once('open',r));const s=(method,params={})=>new Promise(r=>{const id=++i;m.set(id,r);w.send(JSON.stringify({id,method,params}))});await s('Runtime.enable');const r=await s('Runtime.evaluate',{expression:`(()=>{const b=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').includes('个子代理'));if(b&&b.getAttribute('aria-expanded')!=='true')b.click();return new Promise(res=>setTimeout(()=>res({expanded:b?.getAttribute('aria-expanded'),trees:[...document.querySelectorAll('[role="tree"]')].map(x=>x.outerHTML.slice(0,20000)),listboxes:[...document.querySelectorAll('[role="listbox"],[role="menu"]')].map(x=>x.outerHTML.slice(0,10000))}),500))})()`,awaitPromise:true,returnByValue:true});console.log(JSON.stringify(r.result.value,null,2));w.close()
