import fs from 'node:fs/promises'
import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9352/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('127.0.0.1:3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=0,pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);p(m.result)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise(r=>{const n=++id;pending.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const ev=async expression=>(await send('Runtime.evaluate',{expression,returnByValue:true})).result.value;const wait=ms=>new Promise(r=>setTimeout(r,ms));await send('Runtime.enable');await send('Page.enable');await send('Emulation.setEmulatedMedia',{features:[{name:'prefers-reduced-motion',value:'no-preference'}]});await ev(`localStorage.removeItem('dsh-theme-cutout-clash.skin');localStorage.removeItem('dsh-theme-acid-noir.skin')`);await send('Page.reload',{ignoreCache:true});const timeline=[];let elapsed=0;for(const ms of [280,400,520,680,900]){await wait(ms-elapsed);elapsed=ms;timeline.push(await ev(`(()=>{const p=document.querySelector('.cc-plate');return{ms:${ms},saved:localStorage.getItem('dsh-theme-cutout-clash.skin'),theme:document.body.getAttribute('data-cutout-clash'),stage:p?.getAttribute('data-stage')||null,gen:p?.getAttribute('data-generation')||null,visible:p?getComputedStyle(p).visibility:null}})()`))}await fs.writeFile('D:/Resources/DSH_PRJ/DevPlugins/dsh-theme-cutout-clash/artifacts/host-restored-startup.json',JSON.stringify(timeline,null,2));console.log(JSON.stringify(timeline,null,2));ws.close()
