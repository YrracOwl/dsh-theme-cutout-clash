import fs from 'node:fs/promises'
import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const p=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(x=>x.type==='page'&&x.url.includes('3080'));const w=new WebSocket(p.webSocketDebuggerUrl);let i=0,m=new Map();w.on('message',x=>{let a=JSON.parse(String(x));if(a.id&&m.has(a.id)){let q=m.get(a.id);m.delete(a.id);a.error?q.reject(new Error(a.error.message)):q.resolve(a.result)}});await new Promise(r=>w.once('open',r));const s=(method,params={})=>new Promise((resolve,reject)=>{let id=++i;m.set(id,{resolve,reject});w.send(JSON.stringify({id,method,params}))});const ev=async exp=>(await s('Runtime.evaluate',{expression:exp,returnByValue:true})).result.value;const wait=ms=>new Promise(r=>setTimeout(r,ms));await s('Runtime.enable');const out={};for(const theme of ['cutout-clash','cutout-clash-night','cutout-clash-pop']){await ev(`localStorage.setItem('dsh.theme.preference.v1',JSON.stringify({version:1,theme:${JSON.stringify(theme)},owner:'dsh-theme-cutout-clash'}));localStorage.setItem('dsh-theme-cutout-clash.motion','off');location.reload()`);await wait(1200);await ev(`document.querySelector('[aria-label="打开侧边栏"]')?.click()`);await wait(250);await ev(`(()=>{const n='开发 Persona 5 风格 DSH 主题';const t=[...document.querySelectorAll('*')].find(e=>(e.textContent||'').trim()===n);t?.closest('[role="treeitem"]')?.click()})()`);await wait(600);out[theme]=await ev(`(()=>{const items=[...document.querySelectorAll('[data-pane="sidebar"] [role="treeitem"][aria-selected="true"],[data-pane="sidebar"] [role="treeitem"][aria-expanded="true"]')];return items.map(e=>{const c=getComputedStyle(e);return{text:(e.textContent||'').replace(/\\s+/g,' ').trim(),color:c.color,bg:c.backgroundColor,children:[...e.querySelectorAll('*')].filter(x=>(x.textContent||'').trim()).slice(0,5).map(x=>({text:(x.textContent||'').trim(),color:getComputedStyle(x).color}))}})})()`)}await fs.writeFile('D:/Resources/DSH_PRJ/DevPlugins/dsh-theme-cutout-clash/artifacts/selected-variants-v040.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));w.close()
