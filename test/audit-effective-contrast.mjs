import fs from 'node:fs/promises'
import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('127.0.0.1:3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=0,pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise((resolve,reject)=>{const n=++id;pending.set(n,{resolve,reject});ws.send(JSON.stringify({id:n,method,params}))});const ev=async expression=>{const r=await send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text);return r.result.value};const wait=ms=>new Promise(r=>setTimeout(r,ms));await send('Runtime.enable');await ev(`localStorage.setItem('dsh-theme-cutout-clash.skin','cutout-clash-night');location.reload()`);await wait(1800);await ev(`document.querySelector('[aria-label="打开侧边栏"]')?.click()`);await wait(350);await ev(`(()=>{const needle='开发 Persona 5 风格 DSH 主题';const t=[...document.querySelectorAll('*')].find(e=>(e.textContent||'').trim()===needle);t?.closest('[role="treeitem"]')?.click()})()`);await wait(2200);
const audit=await ev(`(()=>{const parse=s=>{const m=s?.match(/rgba?\\((\\d+),\\s*(\\d+),\\s*(\\d+)(?:,\\s*([\\d.]+))?/);return m?[+m[1],+m[2],+m[3],m[4]===undefined?1:+m[4]]:null},lum=c=>{const a=c.slice(0,3).map(v=>{v/=255;return v<=.04045?v/12.92:Math.pow((v+.055)/1.055,2.4)});return.2126*a[0]+.7152*a[1]+.0722*a[2]},ratio=(a,b)=>{const x=lum(a),y=lum(b);return(Math.max(x,y)+.05)/(Math.min(x,y)+.05)},bgOf=el=>{for(let n=el;n;n=n.parentElement){const c=parse(getComputedStyle(n).backgroundColor);if(c&&c[3]>=.95)return c}return parse(getComputedStyle(document.body).backgroundColor)};const list=[];for(const el of document.querySelectorAll('button,[role="button"],[role="dialog"] *,[data-tool] *,[data-variant="think"] *,.md-code-block *,pre *')){const r=el.getBoundingClientRect(),text=(el.childElementCount?Array.from(el.childNodes).filter(n=>n.nodeType===3).map(n=>n.textContent).join(' '):el.textContent||'').replace(/\\s+/g,' ').trim().slice(0,100);if(!text||r.width<2||r.height<2||el.closest('[aria-hidden="true"]'))continue;const c=getComputedStyle(el),fg=parse(c.color),bg=bgOf(el);if(!fg||!bg)continue;const q=ratio(fg,bg);if(q<4.5)list.push({text,ratio:q,color:c.color,bg:getComputedStyle(el).backgroundColor,effectiveBg:'rgb('+bg.slice(0,3).join(', ')+')',tag:el.tagName,role:el.getAttribute('role'),cls:String(el.className).slice(0,100),tool:el.closest('[data-tool]')?.getAttribute('data-tool')||null})}return{theme:document.body.getAttribute('data-cutout-clash'),counts:{tools:document.querySelectorAll('[data-tool]').length,code:document.querySelectorAll('.md-code-block,pre').length,dialogs:document.querySelectorAll('[role="dialog"]').length},bad:list.sort((a,b)=>a.ratio-b.ratio).slice(0,100)}})()`);
const buttons=await ev(`[...document.querySelectorAll('button')].map(b=>({text:(b.textContent||'').replace(/\\s+/g,' ').trim().slice(0,100),aria:b.getAttribute('aria-label')})).filter(x=>/子代理|详情|subagent/i.test(x.text)||/子代理|subagent/i.test(x.aria||'')).slice(0,30)`);const out={audit,buttons};await fs.writeFile('D:/Resources/DSH_PRJ/DevPlugins/dsh-theme-cutout-clash/artifacts/effective-contrast-before.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));ws.close()
