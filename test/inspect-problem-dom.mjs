import fs from 'node:fs/promises'
import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('127.0.0.1:3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=0,pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise((resolve,reject)=>{const n=++id;pending.set(n,{resolve,reject});ws.send(JSON.stringify({id:n,method,params}))});const ev=async expression=>{const r=await send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text);return r.result.value};const wait=ms=>new Promise(r=>setTimeout(r,ms));await send('Runtime.enable');const before=await ev(`(()=>{const file=document.querySelector('._fileMention_1nba0_249'),tree=document.querySelector('.P4kPIW_file'),sub=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').includes('个子代理'));return{file:file?.outerHTML,tree:tree?.outerHTML,fileParents:file?[file.parentElement?.outerHTML.slice(0,1200),file.parentElement?.parentElement?.outerHTML.slice(0,1600)]:null,treeParents:tree?[tree.parentElement?.outerHTML.slice(0,1200),tree.parentElement?.parentElement?.outerHTML.slice(0,1600)]:null,sub:sub?.outerHTML}})()`);await ev(`(()=>{const b=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').includes('个子代理'));b?.click();return !!b})()`);await wait(700);const after=await ev(`(()=>({dialogs:[...document.querySelectorAll('[role="dialog"]')].map(d=>d.outerHTML.slice(0,12000)),overlays:[...document.querySelectorAll('[role="menu"],[role="listbox"],[data-shell-overlay]')].map(d=>d.outerHTML.slice(0,4000)),bodyText:document.body.innerText.slice(-4000)}))()`);const out={before,after};await fs.writeFile('D:/Resources/DSH_PRJ/DevPlugins/dsh-theme-cutout-clash/artifacts/problem-dom.json',JSON.stringify(out,null,2));console.log(JSON.stringify({before,dialogs:after.dialogs.map(x=>x.slice(0,1000)),bodyText:after.bodyText},null,2));ws.close()
