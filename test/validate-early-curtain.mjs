import fs from 'node:fs/promises'
import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let seq=0,pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))});const ev=async expression=>(await send('Runtime.evaluate',{expression,returnByValue:true})).result.value;await send('Runtime.enable');await send('Page.enable');await ev(`localStorage.setItem('dsh.theme.preference.v1',JSON.stringify({version:1,theme:'cutout-clash-night',owner:'dsh-theme-cutout-clash'}));localStorage.setItem('dsh-theme-cutout-clash.motion','full')`);await send('Page.addScriptToEvaluateOnNewDocument',{source:`window.__ccTimeline=[];const original=Node.prototype.appendChild;Node.prototype.appendChild=function(node){const out=original.call(this,node);if(node?.id==='root')window.__ccTimeline.push({t:performance.now(),event:'root-appended',curtain:!!document.getElementById('dsh-cutout-transition-root')});return out};const append=Element.prototype.append;Element.prototype.append=function(...nodes){const out=append.apply(this,nodes);for(const node of nodes)if(node?.id==='dsh-cutout-transition-root')window.__ccTimeline.push({t:performance.now(),event:'curtain-appended',rootChildren:document.getElementById('root')?.childElementCount||0,frame:!!document.querySelector('[data-dsh-frame]')});return out};document.addEventListener('DOMContentLoaded',()=>window.__ccTimeline.push({t:performance.now(),event:'dom-content-loaded',curtain:!!document.getElementById('dsh-cutout-transition-root'),rootChildren:document.getElementById('root')?.childElementCount||0}));`});await send('Page.reload',{ignoreCache:true});await new Promise(r=>setTimeout(r,1800));const out=await ev(`(()=>{const plate=document.querySelector('.cc-plate'),frame=document.querySelector('[data-dsh-frame]'),details=document.querySelector('[data-pane="details"]');const pi=plate?.getBoundingClientRect(),fi=frame?.getBoundingClientRect(),di=details?.getBoundingClientRect();return{timeline:window.__ccTimeline,theme:document.body.getAttribute('data-cutout-clash'),curtain:!!document.getElementById('dsh-cutout-transition-root'),plate:plate?{position:getComputedStyle(plate).position,z:getComputedStyle(plate).zIndex,rect:{x:pi.x,y:pi.y,w:pi.width,h:pi.height}}:null,frame:fi?{x:fi.x,y:fi.y,w:fi.width,h:fi.height}:null,details:di?{x:di.x,y:di.y,w:di.width,h:di.height}:null}})()`);await fs.writeFile('D:/Resources/DSH_PRJ/DevPlugins/dsh-theme-cutout-clash/artifacts/early-curtain-v040.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));ws.close()
