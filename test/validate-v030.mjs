import fs from 'node:fs/promises'
import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let seq=0,pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise((resolve,reject)=>{const id=++seq;pending.set(id,{resolve,reject});ws.send(JSON.stringify({id,method,params}))});const ev=async expression=>{const r=await send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.exception?.description||r.exceptionDetails.text);return r.result.value};const wait=ms=>new Promise(r=>setTimeout(r,ms));await send('Runtime.enable');await send('Page.enable');
await ev(`localStorage.removeItem('dsh-theme-cutout-clash.skin');localStorage.removeItem('dsh-theme-acid-noir.skin');localStorage.setItem('dsh.theme.preference.v1',JSON.stringify({version:1,theme:'cutout-clash-night',owner:'dsh-theme-cutout-clash'}));localStorage.setItem('dsh-theme-cutout-clash.motion','full');location.reload()`);const startup=[];let elapsed=0;for(const ms of [350,650,850,1050,1300]){await wait(ms-elapsed);elapsed=ms;startup.push(await ev(`(()=>{const p=document.querySelector('.cc-plate');return{ms:${ms},reduced:matchMedia('(prefers-reduced-motion: reduce)').matches,theme:document.body.getAttribute('data-cutout-clash'),stage:p?.getAttribute('data-stage')||null,display:p?getComputedStyle(p).display:null,visible:p?getComputedStyle(p).visibility:null,shared:localStorage.getItem('dsh.theme.preference.v1'),motion:localStorage.getItem('dsh-theme-cutout-clash.motion')}})()`))}
await ev(`location.reload()`);await wait(1500);const restart=await ev(`(()=>({theme:document.body.getAttribute('data-cutout-clash'),shared:JSON.parse(localStorage.getItem('dsh.theme.preference.v1')||'null'),legacy:{cutout:localStorage.getItem('dsh-theme-cutout-clash.skin'),acid:localStorage.getItem('dsh-theme-acid-noir.skin')},motion:localStorage.getItem('dsh-theme-cutout-clash.motion')}))()`);
await ev(`document.querySelector('[aria-label="打开侧边栏"]')?.click()`);await wait(300);await ev(`(()=>{const n='开发 Persona 5 风格 DSH 主题';const t=[...document.querySelectorAll('*')].find(e=>(e.textContent||'').trim()===n);t?.closest('[role="treeitem"]')?.click()})()`);await wait(1800);await ev(`(()=>{const b=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').includes('个子代理'));if(b&&b.getAttribute('aria-expanded')!=='true')b.click()})()`);await wait(400);
const contrast=await ev(`(()=>{const parse=s=>{const m=s?.match(/rgba?\\((\\d+),\\s*(\\d+),\\s*(\\d+)(?:,\\s*([\\d.]+))?/);return m?[+m[1],+m[2],+m[3],m[4]===undefined?1:+m[4]]:null},lum=c=>{const a=c.slice(0,3).map(v=>{v/=255;return v<=.04045?v/12.92:Math.pow((v+.055)/1.055,2.4)});return.2126*a[0]+.7152*a[1]+.0722*a[2]},ratio=(a,b)=>{const x=lum(a),y=lum(b);return(Math.max(x,y)+.05)/(Math.min(x,y)+.05)},bgOf=el=>{for(let n=el;n;n=n.parentElement){const c=parse(getComputedStyle(n).backgroundColor);if(c&&c[3]>=.95)return c}return parse(getComputedStyle(document.body).backgroundColor)};const bad=[];for(const el of document.querySelectorAll('[role="tree"][aria-label="子代理会话"] *,[data-goal-bar] *,[data-produced-files-row] *, .md-code-block *,pre *')){const text=(el.childElementCount?Array.from(el.childNodes).filter(n=>n.nodeType===3).map(n=>n.textContent).join(' '):el.textContent||'').replace(/\\s+/g,' ').trim().slice(0,80),r=el.getBoundingClientRect();if(!text||r.width<2||r.height<2)continue;const c=getComputedStyle(el),fg=parse(c.color),bg=bgOf(el);if(fg&&bg){const q=ratio(fg,bg);if(q<4.5)bad.push({text,ratio:q,color:c.color,bg:'rgb('+bg.slice(0,3).join(',')+')',cls:String(el.className).slice(0,80)})}}return{bad:bad.slice(0,60),counts:{subagent:document.querySelectorAll('[role="tree"][aria-label="子代理会话"] [role="treeitem"]').length,files:document.querySelectorAll('[data-produced-files-row] button').length,code:document.querySelectorAll('.md-code-block,pre').length},overflow:document.documentElement.scrollWidth>innerWidth+1}})()`);
const out={startup,restart,contrast};await fs.writeFile('D:/Resources/DSH_PRJ/DevPlugins/dsh-theme-cutout-clash/artifacts/validation-v030.json',JSON.stringify(out,null,2));console.log(JSON.stringify(out,null,2));ws.close()
