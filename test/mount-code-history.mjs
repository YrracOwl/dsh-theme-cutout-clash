import WebSocket from 'file:///C:/Users/CarryWho/.dsh/profiles/web/node_modules/ws/wrapper.mjs'
const page=(await fetch('http://127.0.0.1:9353/json').then(r=>r.json())).find(p=>p.type==='page'&&p.url.includes('3080'));const ws=new WebSocket(page.webSocketDebuggerUrl);let id=0,pending=new Map();ws.on('message',raw=>{const m=JSON.parse(String(raw));if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);p(m.result)}});await new Promise(r=>ws.once('open',r));const send=(method,params={})=>new Promise(r=>{const n=++id;pending.set(n,r);ws.send(JSON.stringify({id:n,method,params}))});const ev=async expression=>(await send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true})).result.value;const wait=ms=>new Promise(r=>setTimeout(r,ms));await send('Runtime.enable');const theme=process.argv[2];if(theme){await ev(`localStorage.setItem('dsh.theme.preference.v1',JSON.stringify({version:1,theme:${JSON.stringify(process.argv[2])},owner:'dsh-theme-cutout-clash'}));location.reload()`);await wait(1500)}for(let n=0;n<6;n++){const clicked=await ev(`(()=>{const b=[...document.querySelectorAll('button')].find(b=>(b.textContent||'').trim()==='加载更早');if(b){b.click();return true}return false})()`);if(!clicked)break;await wait(650)}const out=await ev(`(()=>{const sc=document.querySelector('[data-conversation-scroll]');if(sc)sc.scrollTop=0;return{buttons:[...document.querySelectorAll('button')].filter(b=>(b.textContent||'').trim()==='加载更早').length,messages:document.querySelectorAll('[data-chat-flow-key]').length,codes:document.querySelectorAll('.md-code-block').length,html:document.querySelector('.md-code-block')?.outerHTML.slice(0,3500)||null}})()`);await wait(500);out.codesAfter=await ev(`document.querySelectorAll('.md-code-block').length`);console.log(JSON.stringify(out,null,2));ws.close()
